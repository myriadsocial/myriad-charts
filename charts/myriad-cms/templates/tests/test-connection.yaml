apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myriad-cms.fullname" . }}-test-connection"
  labels:
    {{- include "myriad-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "myriad-cms.fullname" . }}:{{ .Values.service.port }}"]
  restartPolicy: Never
