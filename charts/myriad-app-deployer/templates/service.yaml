apiVersion: v1
kind: Service
metadata:
  name: {{ include "myriad-app-deployer.fullname" . }}
  labels:
    {{- include "myriad-app-deployer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myriad-app-deployer.selectorLabels" . | nindent 4 }}
