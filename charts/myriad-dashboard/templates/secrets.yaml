{{- if .Values.config.apiKey }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "myriad-dashboard.apiSecretName" . }}
type: Opaque
data:
  key: {{ .Values.config.apiKey | b64enc }}

---
{{- end }}
